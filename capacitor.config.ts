import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'colossio-assignment',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
